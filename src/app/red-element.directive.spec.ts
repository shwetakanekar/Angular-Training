import { RedElementDirective } from './red-element.directive';

describe('RedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new RedElementDirective();
    expect(directive).toBeTruthy();
  });
});
